# 🤖 Telegram Media Downloader Bot

Telegram бот для скачивания медиафайлов высокого качества с Pinterest, TikTok и Instagram без водяных знаков.

## ✨ Возможности

- 📸 **Pinterest** - скачивание пинов и видео
- 🎵 **TikTok** - скачивание видео без водяных знаков  
- 📷 **Instagram** - скачивание фото, видео и reels
- 🎯 **Максимальное качество** - автоматическое определение лучшего разрешения
- ⚡ **Быстрая обработка** - мгновенная загрузка и отправка
- 🌐 **24/7 работа** - развертывание на serverless платформе

## 🚀 Быстрый старт

### 1. Клонирование репозитория
```bash
git clone https://github.com/your-username/telegram-media-bot.git
cd telegram-media-bot
```

### 2. Настройка окружения
```bash
# Создаем виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Устанавливаем зависимости
pip install -r requirements.txt
```

### 3. Настройка переменных окружения
```bash
# Копируем шаблон
cp .env.example .env

# Редактируем .env файл
nano .env
```

Обязательно заполните:
- `TELEGRAM_BOT_TOKEN` - токен вашего бота от @BotFather

### 4. Запуск в режиме разработки
```bash
python src/bot/main.py
```

## 🌐 Развертывание на AWS Lambda

### 1. Установка Serverless Framework
```bash
npm install -g serverless
```

### 2. Настройка AWS
```bash
# Устанавливаем AWS CLI
aws configure
```

### 3. Развертывание
```bash
cd serverless
serverless deploy --stage production
```

### 4. Настройка Webhook
После развертывания получите URL функции и установите webhook:
```bash
curl -X POST "https://api.telegram.org/botYOUR_TOKEN/setWebhook" \
  -H "Content-Type: application/json" \
  -d "{\"url\": \"YOUR_FUNCTION_URL/webhook\", \"drop_pending_updates\": true}"
```

## 🔄 Автоматическое развертывание (CI/CD)

Для автоматического развертывания при пуше в main:

1. Перейдите в Settings → Secrets and variables → Actions в GitHub
2. Добавьте секреты:
   - `TELEGRAM_BOT_TOKEN` - токен бота
   - `AWS_ACCESS_KEY_ID` - ключ доступа AWS
   - `AWS_SECRET_ACCESS_KEY` - секретный ключ AWS

3. Сделайте push в main ветку - автоматическое развертывание начнется!

## 📁 Структура проекта

```
telegram-media-bot/
├── src/
│   ├── bot/
│   │   ├── main.py              # Основной файл бота
│   │   └── handlers/            # Обработчики команд и сообщений
│   ├── services/
│   │   └── media_downloader.py  # Сервис скачивания медиа
│   └── config/
│       └── settings.py          # Конфигурация
├── serverless/
│   ├── handler.py               # Entry point для serverless
│   └── serverless.yml           # Конфигурация развертывания
├── .github/workflows/
│   └── deploy.yml               # CI/CD pipeline
├── requirements.txt             # Зависимости Python
├── .env.example                 # Шаблон переменных окружения
└── README.md                    # Этот файл
```

## 🛠️ Технологии

- **aiogram 3** - современный фреймворк для Telegram ботов
- **yt-dlp** - скачивание видео с различных платформ
- **instaloader** - работа с Instagram
- **Serverless Framework** - развертывание на AWS Lambda
- **GitHub Actions** - CI/CD автоматизация

## 📝 Поддерживаемые ссылки

### Pinterest
```
https://pinterest.com/pin/123456789/
https://pin.it/123456789
```

### TikTok
```
https://tiktok.com/@username/video/123456789
```

### Instagram
```
https://instagram.com/p/123456789/
https://instagram.com/reel/123456789/
```

## ⚙️ Настройка

### Основные параметры (.env)
- `TELEGRAM_BOT_TOKEN` - токен бота
- `LOG_LEVEL` - уровень логирования (INFO/DEBUG/ERROR)
- `MAX_FILE_SIZE_MB` - максимальный размер файла (по умолчанию 50MB)
- `TIMEOUT_SECONDS` - таймаут загрузки (по умолчанию 30 сек)

### Продвинутые настройки
- `PROXY_URL` - URL прокси если необходимо
- `WEBHOOK_URL` - URL для webhook (устанавливается автоматически)

## 🔧 Локальная разработка

Для тестирования бота локально:

1. Установите зависимости
2. Создайте `.env` файл с токеном
3. Запустите бота:
```bash
python src/bot/main.py
```

## 📊 Мониторинг

Бот автоматически логирует:
- Успешные загрузки
- Ошибки и предупреждения
- Статистику использования

Логи сохраняются в `logs/bot.log` (ротация каждые 10MB)

## 🐛 Устранение проблем

### Частые проблемы:

1. **Бот не отвечает**
   - Проверьте токен в `.env`
   - Убедитесь что бот запущен
   - Проверьте логи

2. **Не скачивается медиа**
   - Проверьте доступность ссылки
   - Убедитесь что медиа не удалено
   - Проверьте размер файла (< 50MB)

3. **Ошибка при развертывании**
   - Проверьте AWS настройки
   - Убедитесь что все секреты добавлены в GitHub
   - Проверьте права доступа

## 📄 Лицензия

MIT License - свободное использование и модификация

## 🤝 Вклад

Pull requests приветствуются! Основные направления:
- Поддержка новых платформ
- Улучшение качества скачивания
- Оптимизация производительности

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте этот README
2. Посмотрите логи
3. Создайте Issue в GitHub

---

**⭐ Если проект полезен, поставьте звезду!**
