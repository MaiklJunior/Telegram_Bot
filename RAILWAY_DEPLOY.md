# 🚀 Развертывание на Railway

## 📋 Подготовка

1. **Установите Railway CLI**
```bash
npm install -g @railway/cli
```

2. **Войдите в Railway**
```bash
railway login
```

## 🚀 Развертывание

### 1. Создайте новый проект
```bash
railway new
```

### 2. Загрузите код
```bash
railway up
```

### 3. Настройте переменные окружения
В Railway Dashboard → Settings → Variables добавьте:
- `TELEGRAM_BOT_TOKEN` - токен вашего бота
- `PYTHON_VERSION` - `3.11`
- `PORT` - `8080`

### 4. Развертывание
```bash
railway deploy
```

## 🔧 Настройка Webhook

После развертывания получите URL проекта и установите webhook:

```bash
# Замените YOUR_URL на URL вашего Railway проекта
curl -X POST "https://api.telegram.org/botВАШ_ТОКЕН/setWebhook" \
  -H "Content-Type: application/json" \
  -d "{\"url\": \"YOUR_URL/webhook\", \"drop_pending_updates\": true}"
```

## 📊 Структура проекта

```
├── railway.py          # FastAPI сервер для Railway
├── Procfile           # Команда запуска
├── railway.toml       # Конфигурация Railway
├── requirements.txt   # Зависимости
└── src/              # Исходный код бота
```

## 🧪 Тестирование

- **Health check:** `https://your-app.railway.app/health`
- **Webhook:** `https://your-app.railway.app/webhook`
- **Root:** `https://your-app.railway.app/`

## ✅ Преимущества Railway

- 🐍 **Поддержка Python** - лучше чем Vercel
- ⚡ **Быстрое развертывание** - автоматическое
- 💰 **Free tier** - 500 часов/месяц
- 🔧 **Простая настройка** - минимум конфигурации
- 📊 **Мониторинг** - встроенные логи

## 🆘 Если возникнут проблемы

1. **Проверьте логи:** Railway Dashboard → Logs
2. **Проверьте переменные:** Settings → Variables
3. **Проверьте домен:** должен быть доступен
4. **Переразверните:** `railway deploy`

---

**🎯 Railway - лучший выбор для Telegram ботов на Python!**
