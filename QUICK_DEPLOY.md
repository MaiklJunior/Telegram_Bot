# 🚀 Быстрое развертывание Telegram Bot

## 📋 Что уже готово:
- ✅ Весь код бота создан
- ✅ Зависимости настроены  
- ✅ Vercel CLI установлен и авторизован
- ✅ Файлы для развертывания подготовлены

## 🎯 Следующие шаги:

### 1. Развертывание на Vercel (рекомендую)

Выполните в терминале:
```bash
cd "c:\Users\Miha2003\Documents\Telegram_Bot"
vercel --prod
```

Во время развертывания:
- Выберите "Link to existing project" или создайте новый
- Подтвердите настройки
- Дождитесь завершения развертывания

### 2. Настройка переменных окружения

После развертывания:
1. Перейдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в Settings → Environment Variables
4. Добавьте переменную:
   - Name: `TELEGRAM_BOT_TOKEN`
   - Value: ваш токен от @BotFather
5. Нажмите Save

### 3. Установка Webhook

Полученный URL будет выглядеть: `https://your-project.vercel.app/webhook`

Установите webhook:
```bash
curl -X POST "https://api.telegram.org/botВАШ_ТОКЕН/setWebhook" \
  -H "Content-Type: application/json" \
  -d "{\"url\": \"https://your-project.vercel.app/webhook\", \"drop_pending_updates\": true}"
```

### 4. Проверка

Проверьте статус webhook:
```bash
curl "https://api.telegram.org/botВАШ_ТОКЕН/getWebhookInfo"
```

## 🎉 Готово!

Бот будет работать 24/7 на Vercel бесплатно. Лимиты:
- 100GB bandwidth в месяц
- Неограниченное количество запросов
- Автоматическое масштабирование

## 🔄 Автоматические обновления

Для автоматического развертывания при изменениях:
1. Подключите Git репозиторий к Vercel
2. Каждый push в main ветку будет автоматически развертываться

## 🆘 Если возникнут проблемы

1. **Проверьте логи:** Vercel Dashboard → Functions → Logs
2. **Проверьте токен:** Убедитесь что TELEGRAM_BOT_TOKEN правильный
3. **Проверьте webhook:** Убедитесь что URL доступен

## 📱 Тестирование бота

После настройки:
1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Отправьте ссылку на медиа для тестирования

---

**🎯 Бот готов к использованию!**
